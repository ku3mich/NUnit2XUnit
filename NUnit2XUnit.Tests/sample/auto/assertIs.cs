public class TestClass
{
    public void TestCase()
    {
        Assert.IsTrue(true);
        Assert.IsFalse(true);
        Assert.IsNull(null);
        Assert.IsNotNull(null);
        Assert.IsNotNaN(0);
        Assert.IsNaN(0);
        Assert.IsEmpty(0);
        Assert.IsNotEmpty(0);
    }
}

