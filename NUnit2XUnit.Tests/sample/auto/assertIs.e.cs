public class TestClass
{
    public void TestCase()
    {
        Assert.True(true);
        Assert.False(true);
        Assert.Null(null);
        Assert.NotNull(null);
        Assert.NotNaN(0);
        Assert.NaN(0);
        Assert.Empty(0);
        Assert.NotEmpty(0);
    }
}

